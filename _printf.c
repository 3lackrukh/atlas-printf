#include <stdio.h>
#include <string.h>
#include "main.h"

// we will have to include our own header file and inside we will need our table of format specifiers and functions and prototypes

_printf(const char *format, ...)
{
	va_list santa_bag;
	int i;
	int char_print = 0;

	va_start (santa_bag, format);

}
	
